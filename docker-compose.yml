services:
  rag-app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - frontend/local_rag/.env
    volumes:
      - rag_uploads:/app/frontend/local_rag/data/uploads
      - rag_faiss:/app/frontend/local_rag/data/faiss_index
    restart: unless-stopped

volumes:
  rag_uploads:
  rag_faiss:
