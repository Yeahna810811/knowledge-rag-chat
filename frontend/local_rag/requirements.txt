# Web API
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart>=0.0.12

# Config
pydantic-settings>=2.6.0
python-dotenv>=1.0.1

# LangChain + observability
langchain>=0.3.7
langchain-core>=0.3.15
langchain-community>=0.3.7
langchain-openai>=0.2.0
langchain-huggingface>=0.1.0
langchain-text-splitters>=0.3.2
langsmith>=0.1.147

# Embeddings (local)
sentence-transformers>=3.3.0

# Vector store
faiss-cpu>=1.9.0

# Database (conversation persistence: SQLAlchemy 2.x + MySQL 8)
SQLAlchemy>=2.0,<3.0
PyMySQL>=1.1,<2.0
# MySQL 8 默认 caching_sha2_password 认证，pymysql 需要 cryptography 才能握手
cryptography>=42.0

# Cache + rate limit (Redis; 只做加速，不是数据源)
redis>=5,<7

# Tests
# fakeredis 让 tests/test_redis.py 在不需要 Redis 服务的情况下跑完单元用例。
# 注意它不支持 Lua(EVAL)，所以限流的 Lua 分支只有连真实 Redis 时才覆盖得到。
fakeredis>=2.20
# httpx 的 ASGITransport 让 tests/test_async_sse.py 能直接向 ASGI app 发请求，
# 不必真的起一个 uvicorn 进程——SSE 的字节级帧格式也能原样校验。
# （SSE 本身是标准库级别的手写实现，不需要 sse-starlette 之类的额外依赖。）
httpx>=0.27

# Document loaders
pypdf>=5.1.0
docx2txt>=0.8
unstructured>=0.16.0
markdown
