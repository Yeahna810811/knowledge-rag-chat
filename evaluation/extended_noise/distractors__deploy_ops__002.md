召回块数」如果被塞满了上限，说明 top_k 设小了；
如果长期远小于上限，说明知识库覆盖不足。

链路追踪在 LLM 应用里尤其重要，因为一次问答会经过检索、重排、生成、
后处理多个阶段，只看总延迟无法定位是哪一段慢。给每个阶段打 span，
能直接看出「慢在检索还是慢在生成」。

## 日志脱敏

用户提问和模型输出都可能包含敏感信息。落日志前必须做脱敏处理，
手机号、身份证、邮箱、密钥都要打码。这一条在金融和医疗场景下是合规硬要求，
不是可选的优化项。