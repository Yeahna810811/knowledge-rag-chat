生成描述，
再把描述文本入库。直接把图片丢给嵌入模型，得到的向量语义不可控。

## 评测方法论

检索评测要区分「能不能召回」和「排在第几位」。
召回率（Recall@K）只看前 K 个里有没有，命中率上去了不代表排序好；
MRR 和 NDCG 才反映排序质量。只看单一指标容易得出错误结论。

还有一点常被忽略：评测集必须包含难负样本。如果语料里只有互相无关的文档，
随便一个检索器都能拿高分，这样的评测结果没有参考价值。