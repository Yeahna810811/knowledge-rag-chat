tion/benchmark_runtime.py`）

54 chunk 语料，40 条查询：

| 指标 | BM25 稀疏路 | 稠密向量路 | 倍数 |
|---|---|---|---|
| 冷启动（加载模型） | **0 ms** | 360 ms | — |
| 入库 54 chunk | **8.12 ms** | 6735.65 ms | **829×** |
| 单条查询延迟 | **0.12 ms** | 21.62 ms | **181×** |
| 重启后重建索引 | 85 ms | — | — |

**诚实说明**：稠密路的绝对值偏慢，因为用的是 numpy 手写 BERT 推理
（生产有 torch/ONNX 加速）。但「入库要跑 N 次 BERT 前向」这个
复杂度差异是结构性的，量级差距不会消失。冷启动 360ms 还是**权重已在
系统缓存**的情况下；首次部署还要额外下载约 95MB 模型文件。

---

## 七、⚠️ 必须声明的实验局限